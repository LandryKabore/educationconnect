name: Deploy EduFaso to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Build
        env:
          GITHUB_PAGES: "true"
          VITE_SUPABASE_PROJECT_ID: uszmpknbnpsenoahivkf
          VITE_SUPABASE_URL: https://uszmpknbnpsenoahivkf.supabase.co
          VITE_SUPABASE_PUBLISHABLE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVzem1wa25ibnBzZW5vYWhpdmtmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODM1Njg1NzIsImV4cCI6MjA5OTE0NDU3Mn0.FNd98h-yZ-ftSeUqOvlqOJfQIcagthOUHXDgF3vKt6Q
        run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
